[Unit]
Description=Ocelot BitTorrent Tracker
Documentation=https://github.com/mgdavisxvs/Ocelot
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=ocelot
Group=ocelot
WorkingDirectory=/var/lib/ocelot
ExecStart=/usr/local/bin/ocelot -c /etc/ocelot/ocelot.conf
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
RestartSec=5s

# GC tuning: collect at 200% live-set growth rather than the default 100%.
# Halves GC frequency on a 2 GB VPS; costs ~1.5× peak heap vs baseline.
Environment=GOGC=200
# Pin to available CPUs; adjust to match your VPS vCPU count.
Environment=GOMAXPROCS=2

# Reload torrent/user/whitelist state on SIGUSR1
KillMode=mixed
SendSIGKILL=yes

# Resource limits
LimitNOFILE=65536
LimitNPROC=4096

# Hardening
NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ProtectHome=yes
ReadWritePaths=/var/lib/ocelot /var/log/ocelot
PrivateDevices=yes
CapabilityBoundingSet=

# Logging — redirect stdout/stderr to journald
StandardOutput=journal
StandardError=journal
SyslogIdentifier=ocelot

[Install]
WantedBy=multi-user.target
